# Secrets — không commit
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Pipeline artifacts (JSON trung gian debug/audit)
tmp/
# intermediate = output trước khi split, trùng với vector/ (debug only)
data/clean/*/intermediate/
# vector cache (artifact lớn — embed cache theo content-hash)
data/.vector_cache/

# OS
Thumbs.db
.DS_Store